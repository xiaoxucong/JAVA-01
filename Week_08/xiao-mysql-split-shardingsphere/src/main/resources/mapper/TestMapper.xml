<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xiao.split.shardingsphere.mapper.TestMapper">

	  <select id="findAll" resultType="map" >
          select * from t1
      </select>

    <insert id="addsd" parameterType="map" >
        INSERT INTO  `t_order`  (userId, `order_no`, `sum_price`, `createtime`, `paymenttime`,
        `order_status`, `express_no`, `express_name`, `consignee_address`, `consignee_mobile`,
        `consignee_name`, `deleteStatus`)
         VALUE
             (#{userId}, 'order_no1',  '1.00', '2021-03-03 09:38:53',
             '2021-03-03 09:39:13', 1, '1', '1', '1', '1', '1', 0)

    </insert>

    <select id="findOrder" parameterType="map" resultType="map" >
        select id,userId,order_no from `t_order` where userId=#{userId} and id= #{id} limit 1
      </select>
    <update id="update" parameterType="map" >
       update   `t_order`  set `order_no`=#{order_no} where userId=#{userId} and id= #{id}

    </update>
</mapper>

